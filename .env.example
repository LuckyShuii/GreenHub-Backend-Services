# Contract with the infrastructure repo: variable NAMES only.
# Keep in sync with infrastructure/roles/backend/templates/env.j2 -- adding a
# variable means updating both files in the same PR.
# Copy to .env for local dev; never commit real values.

APP_ENV=local

DB_HOST=localhost
DB_PORT=5432
DB_NAME=greener
DB_USER=
DB_PASSWORD=

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=
